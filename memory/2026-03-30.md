# 2026-03-30 工作日志

## 新决策
- 用户改变 GPU 执行策略：后续这个项目中所有需要 GPU 的代码，默认都改为 **纯 Python 脚本** 作为 source of truth。
- 不再默认依赖 Colab / Jupyter Notebook / Gist 作为 GPU 执行主流程。
- 新的默认流程是：
  1. 在项目仓库内维护 `.py` 代码
  2. 提交并推送到 GitHub 仓库
  3. 在有 GPU 的机器上 `git pull`
  4. 直接运行 Python 脚本完成训练/评测
- 当前项目 GitHub 仓库已存在：`https://github.com/systemoutprintlnnnn/dsaa-5009-final`
- 当前默认分支：`main`
- 用户补充了新的硬件约束：主要执行设备是 **Mac M4 24GB**，后续训练/推理代码需要优先考虑 Apple Silicon / MPS 适配，而不是只按 CUDA 设备思路写。

## 已同步更新的文件
- `WORKFLOW.md`：GPU Code Rules 已从 Colab/Gist 流程改成 GitHub repo + pure Python 流程。
- `CHECKPOINTS.md`：CP-05 的 artifact 已改成记录 GitHub repo 与 branch，而不是 notebook gist。
- `TASKS.md`：补充说明 GPU 代码默认通过 GitHub 仓库同步到 GPU 机器执行。

## 影响
- 之前为 CP-05 创建的 notebook/gist 现在不再是主执行路径。
- 之后应优先维护：
  - `scripts/check_training_step.py`
  - `scripts/run_training.py`
  - `scripts/run_evaluation.py`
  等 Python 脚本，而不是先产出 notebook。
- 后续 device 选择逻辑应优先支持：`mps` → `cuda` → `cpu` 或按脚本显式配置，避免写死 CUDA。
