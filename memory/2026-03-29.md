# 2026-03-29 工作日志

## 完成事项

### 1. 工程清理（Audit Fix）
- 新增 `.gitignore`
- 清理已提交的 `__pycache__` / `.pyc`
- 新增 `README.md`
- 明确 `.venv`、依赖安装和脚本运行方式

### 2. 完成 CP-04 模型加载验证
- 编写 `src/models/load_model.py`
- 编写 `scripts/check_model_loading.py`
- 成功完成 FLAN-T5 smoke test
- 生成 `results/metrics/model_check_flan.json`

## 关键结果

### FLAN-T5 smoke test
- model: `google/flan-t5-base`
- added tokens: 5
- embedding resize: 成功
- LoRA 注入: 成功
- trainable ratio: 0.007876

### 当前 special tokens
- `<len_SHORT>`
- `<len_MEDIUM>`
- `<len_LONG>`
- `[SUMMARIZE]`
- `[TOPIC]`

## 当前 checkpoint 状态
- CP-01: 🟢 PASS
- CP-02: 🟢 PASS
- CP-03: 🟢 PASS
- CP-04: 🟢 PASS

## 风险 / 注意事项
- HF Hub 当前是未认证下载，后续大模型加载可能会更慢
- FLAN-T5 有 tied weights warning，目前不阻塞，可后续决定是否静音处理

## 下一步
1. 开始 CP-05 单步训练验证
2. 实现最小训练数据 tokenization 流程
3. 跑通 1 batch forward + backward

## 补充记忆（pre-compaction flush）
- 项目主线已固定为“长度可控对话摘要 + 多任务学习”，当前优先模型是 `google/flan-t5-base`，原因是更适配 `T4 16G GPU` 资源约束。
- 长度分桶规则已固定并沿用：`SHORT <= 15`，`MEDIUM 16–35`，`LONG >= 36`。
- 多任务输入格式已固定：摘要任务 `<len_*> [SUMMARIZE] {dialogue}`，主题任务 `[TOPIC] {dialogue}`。
- GPU 相关代码执行规则已写入 `WORKFLOW.md`：凡是需要 GPU 的训练/评测代码，必须先创建或更新 GitHub Gist，再到 Colab 运行；Gist 文件名必须以 `DSAA5009 Final` 开头；同一目标代码文件只能维护同一个 Gist；对应 Gist URL 要记录到 `CHECKPOINTS.md` artifact。
- `gh` CLI 已验证可直接用于 Gist 管理，账号 `systemoutprintlnnnn` 已登录，token scopes 包含 `gist`。
- 当前恢复工作时的明确下一步仍是 `CP-05`：完成最小训练数据 tokenization / batch 流程、跑通 `1 batch forward + backward`、产出 `results/metrics/training_smoke_test.json`。
- 有一个待确认工程项：误提交的符号链接 `skills/opencli` 是否保留，后续需要检查处理。
- 用户新增明确偏好/规则：今后所有需要 GPU 的代码，都应优先以 Jupyter Notebook (`.ipynb`) 形式产出，方便直接生成 Colab 链接；不要只给 `.py` 脚本版。
